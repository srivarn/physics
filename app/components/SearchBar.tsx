import React from 'react';

const SearchBar = () => {
  return (
    <input
      type="text"
      placeholder="Search..."
      className="px-4 py-2 rounded-md text-black"
    />
  );
};

export default SearchBar;
